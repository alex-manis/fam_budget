NODE_ENV=development
PORT=3000
DATABASE_URL=postgresql://postgres:password@localhost:5432/fambudget
DEFAULT_CURRENCY=ILS

# AI classification (optional — rule engine runs without it)
OPENAI_API_KEY=sk-...
